<html>
  <body>
    <h1>Editer</h1>
<div class="container">

      <div class="row">
        <div class="col">
        <% if @array_gossip.errors.any? %>
        <% @array_gossip.errors.full_messages.each do |message| %>
          <div class="alert alert-warning" role="alert">
            freferfref
          </div>
          <% end %>
        <% end %>
      </div>
      </div>
    </div>


<%= form_for @array_gossip do |message| %>

  <div class="form_group">
    <label>Title</label><br>
    <%= message.text_field :title, class: 'form-control'  %>
  </div>

  <div class="form_group">
    <label>Content</label><br>
    <%= message.text_area :content, class: 'form-control' %>
  </div>
<br><br>
   <div class="form_group">
    <button type="submit" class="btn btn-primary">Modifier</button>
  </div>

<% end %>

  </body>
</html>