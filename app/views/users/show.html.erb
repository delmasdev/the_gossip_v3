<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>
<div class='container'>
  <center><h1>Page de présentation de <%= @array_user.first_name %></h1>
  <br>
    <div>
      <p>
        <u>Prénom</u>: <%= @array_user.first_name %><br>
        <u>Nom</u>: <%=  @array_user.last_name %><br>
        <u>Description</u>: <%=  @array_user.description %><br>
        <u>Age</u>: <%=  @array_user.age %><br>
        <u>Email</u>:<%=  @array_user.email %><br>
        <u>Ville</u>: <%= link_to "#{City.find(@array_user.city_id).name}", city_path(City.find(@array_user.city_id)) %><br>
      </p>
    </div>

  <p>
    <%= link_to "Retour à la page du gossip", :back%>
  </p>
  </center>
</div>